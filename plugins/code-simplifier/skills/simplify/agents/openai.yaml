interface:
  display_name: "Simplify"
  short_description: "Tighten changed code and docs"
  default_prompt: "Use $simplify to tighten recently changed code, comments, and docs without changing behavior."
policy:
  allow_implicit_invocation: true
