interface:
  display_name: "Simplify"
  short_description: "Reduce bloat in changed code and docs"
  default_prompt: "Use $simplify to reduce bloat in recently changed code, comments, and docs without changing behavior."
policy:
  allow_implicit_invocation: true
