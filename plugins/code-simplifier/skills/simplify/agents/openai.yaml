interface:
  display_name: "Simplify"
  short_description: "Simplify changed code with parallel agents"
  default_prompt: "Use $simplify to simplify recently changed code without changing behavior."
policy:
  allow_implicit_invocation: true
