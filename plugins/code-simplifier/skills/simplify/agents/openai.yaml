interface:
  display_name: "Simplify"
  short_description: "Simplify changed code and docs with parallel agents"
  default_prompt: "Use $simplify to simplify recently changed code, comments, and docs without changing behavior."
policy:
  allow_implicit_invocation: true
